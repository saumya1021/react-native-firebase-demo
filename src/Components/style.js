import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  inputLabelStyle: {
    fontSize: 16,
    color: 'grey',
    // marginBottom: 10,
    fontWeight: 'bold',
    marginHorizontal:25
  },
});
